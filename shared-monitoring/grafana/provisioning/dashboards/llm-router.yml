apiVersion: 1

providers:
  - name: 'LLM Router Dashboards'
    orgId: 1
    folder: 'LLM Router'
    folderUid: llm-router
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/llm-router